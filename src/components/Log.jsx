import BookForm from './Book';

const Log = () => {
    return (
        <div>
            <h1>Log</h1>
            <BookForm type="Log" />
            {/* Add logic to display log books */}
        </div>
    );
};

export default Log;